# Microscopy Image Super-Resolution (TensorFlow SRGAN)

This repository contains preprocessing, dataset loading, SRGAN model (generator + discriminator), 
VGG-based perceptual loss, and full training code for microscopy image super-resolution.
